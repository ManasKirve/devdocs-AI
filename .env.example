# DevDocs AI - environment variables (placeholders only, no real values).
# Copy to the relevant .env file and fill in real values during implementation.

# Backend (backend/.env)
XAI_API_KEY=
XAI_MODEL=grok-4.5
CORS_ORIGINS=["http://localhost:5173"]

# Frontend (frontend/.env)
VITE_API_BASE_URL=http://localhost:8000/api/v1
